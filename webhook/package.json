{
  "name": "keyblind-webhook",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "resend": "^4.8.0",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
